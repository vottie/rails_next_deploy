# 目的
本リポジトリは、他のリポジトリで導入したい技術のテストが目的である。

# 依頼内容
- rails_next_deployでgithubにリポジトリを作成
- developブランチを作成
- railsでアプリケーションを作成
- フロントエンドはnext.js
- 開発環境はdevcontainerで作成
- capistranoを導入
- DBはsqlite
- Webサーバーはnginx
- アプリケーションサーバーはpuma
- rspecを使用

## devcontainerの前提
dockerはインストール済、
ベースのイメージはubuntuでTAGは"24.04"、IMAGE IDは20377134ad88
追加するプログラムはDockerfileに記載
以下はaptを使って、必須で追加。バージョンは整合性が取れる限り最新版でインストール
- zsh
- vim
- build-essential
- curl
- wget
- git
oh-my-zshはcurlで取得し、デフォルトの設定でインストール
コメントを書く際は英語で書く
他に必要なソフト、例えばNode.jsなどは適宜インストールすること。

## capistranoの詳細
テスト用環境へのデプロイと本番環境用へのデプロイの２つを有する。
テスト用環境はdevcontainerの開発環境と類似
本番環境はT.B.D.とする。

## アプリケーションの詳細
ユーザー名とパスワードをデータとして持つ。
ログイン画面と、ログイン成功後の画面の２画面を持つ。
ログアウトすると、ログイン画面に戻る。
ユーザー名はアルファベットと英語と記号を使用可能で、6から10文字。
ユーザー名はアルファベットと英語と記号を使用可能で、6から10文字。